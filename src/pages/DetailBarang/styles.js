import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  container: {
    alignContent: "center",
  },
  inputContainer: {
    margin: 10,
  },
  IconDelete: {
    position: "absolute",
    right: 12,
    top: -10,
    zIndex: 10,
  },
});

export default styles;
